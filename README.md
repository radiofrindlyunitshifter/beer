A Database file is needed with those tables and the name "beerStatistic.db":
Create table beers(
                 brand text,
                 year int,
                 beerCount int);

Create table names(
                  name text,
                  year int,
                  nameCount int);
