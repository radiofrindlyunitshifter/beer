A Database file is needed with those tables:
Create table beers(
                 brand text,
                 year int,
                 beerCount int);

Create table names(
                  name text,
                  year int,
                  nameCount int);
